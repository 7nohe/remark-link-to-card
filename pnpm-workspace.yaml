packages:
  - ./*
  - examples/**/*

# pnpm >=10 reads its own settings from here rather than from .npmrc.
ignoreWorkspaceRootCheck: true

# pnpm >=10 does not run dependency build scripts unless they are listed here.
allowBuilds:
  '@biomejs/biome': true
  esbuild: true
  simple-git-hooks: true
